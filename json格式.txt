{
	"type":
		这条数据的类型：
		"read"：从云端读取数据。
		"write"：向云端写入数据。
		"return"：回应请求的数据。当写入无误时，服务端也会返回一条return。
		"error"：发生错误时的回应。
	"message":
		当"type"为"error"时包含此条。
		发出错误消息。
	"database":
		当"type"为"read"或"write"时包含此条。
		请求访问的数据库。
		"course"：包含大学数据库的所有信息，只可读不可写。
		"student"：包含每个学生各自的选课列表。
	"table"：
		当"type"为"read"或"write"时包含此条。
		请求访问的数据表。
		数据库courses下包含"classroom", "course", "department", "instructor", "section", "student"数据表。
		数据库students下包含每个学生以其学号作为数据表名的数据表。
	"primaryKeyValues":[
		当"type"为"read"时包含此条。
		提供读取的记录的主键值。
		特别的，当"primaryKeyValues"为空时读取整个表所有数据。
		数据库course下的所有数据表的主键为：
		classroom：[building：string, room_number：integer]（注意：两个主键）
		course：[course_id：integer]（自增）
		department：[id：integer]（自增）
		instructor：[ID：integer]（自增）
		section：[sec_id：integer]（自增）
		student：[ID：integer]（自增）
	],
	values:[
		当"type"为"write"或者"return"时包含此条。
		包含传输的数据。
		对于自增主键，当"type"为"write"时，不提供该键则视为插入数据，提供该键将视为更新记录。
		{
			"键名": 值
			"键名"：值
			...
		},
		{
			"键名": 值
			"键名"：值
			...
		},
		...
	]
}